<nav class="w-full border-b border-gray-200 bg-white">
  <div class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">

      <%= link_to root_path, class: "flex items-center gap-3 hover:opacity-90" do %>
      <div class="flex items-center justify-center w-8 h-8 rounded-full bg-indigo-600 text-white font-bold">
        ✦
      </div>
      <div class="leading-tight">
      <div class="font-semibold text-gray-900">pluto.ai</div>
      <div class="text-xs text-gray-500">Company Explorer</div>
      </div>
      <% end %>

    <div class="flex items-center gap-6 text-sm font-medium">
      <%= link_to root_path,
          class: "flex items-center gap-2 text-gray-600 hover:text-gray-900" do %>
        <span>Discover</span>
      <% end %>

      <%= link_to lists_path,
          class: "flex items-center gap-2 text-gray-900 border-b-2 border-indigo-600 pb-1" do %>
        <span>My Lists</span>
      <% end %>
    </div>

  </div>
</nav>
