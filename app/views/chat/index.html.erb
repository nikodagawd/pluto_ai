<div class="min-h-screen bg-[#fdf5e6] flex flex-col items-center py-10 px-4">
  <div class="max-w-4xl w-full">

    <div class="text-center mb-12">
      <p class="text-xl md:text-2xl text-black font-semibold flex items-center justify-center gap-2">
        🔍 AI-Powered Discovery
      </p>
      <h1 class="text-5xl md:text-6xl font-bold text-black mt-4">
        Company Explorer
      </h1>
      <p class="text-sm md:text-base text-gray-700 mt-2 max-w-xl mx-auto">
        Ask our AI to find innovative companies. Use natural language to search by sector, location, and more.
      </p>
    </div>

    <div class="w-full bg-white rounded-xl shadow-lg p-8 flex flex-col">
      <p class="text-gray-500 mb-4 text-lg">What would you like to know?</p>

      <%= form_with url: chat_path, method: :post, local: true, data: { turbo: false }, class: "flex w-full mb-6" do |f| %>
        <%= f.text_field :message,
          value: @message,
          placeholder: "Type your question here...",
          class: "flex-grow border border-gray-300 rounded-l-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        <%= f.submit "Send", class: "bg-indigo-600 text-white px-6 py-3 rounded-r-lg hover:bg-indigo-700 cursor-pointer transition-colors" %>
      <% end %>

      <% if @companies.present? %>

        <% if @is_recommendation %>
          <div class="mb-6 bg-amber-50 border-l-4 border-amber-400 p-4 rounded-r-lg">
            <div class="flex">
              <div class="flex-shrink-0">
                <span class="text-amber-400 text-xl">⚠️</span>
              </div>
              <div class="ml-3">
                <p class="text-sm text-amber-800">
                  I couldn't find exact matches for <strong>"<%= @message %>"</strong>, but these similar companies might interest you:
                </p>
              </div>
            </div>
          </div>
        <% else %>
          <h3 class="text-lg font-semibold text-gray-800 mb-4">Top matches for your search:</h3>
        <% end %>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <% @companies.each do |company| %>
            <div class="bg-gray-50 rounded-lg p-4 border border-gray-200 shadow hover:shadow-md transition flex flex-col group">
              <strong class="text-lg text-indigo-900 mb-1 group-hover:text-indigo-600 transition-colors">
                <%= company.company_name %>
              </strong>
              <small class="text-gray-600 mb-2">
                <%= company.sector %> · <%= company.city %>, <%= company.country %>
              </small>
              <p class="text-sm text-gray-700 mb-3 line-clamp-2">
                <%= company.description %>
              </p>
              <small class="text-gray-500 mt-auto pt-2 border-t border-gray-100 flex justify-between">
                <span><%= company.employees %> employees</span>
                <span>Founded in <%= company.founded_year %></span>
              </small>
            </div>
          <% end %>
        </div>

      <% elsif @message.present? %>
        <div class="bg-red-50 border-l-4 border-red-400 p-4 rounded-r-lg">
          <p class="text-red-700 text-sm">No results found for "<%= @message %>". Try another description.</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
