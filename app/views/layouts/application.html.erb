<!DOCTYPE html>
<html>
  <head>
    <title>Pluto AI</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Solo el stylesheet principal -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans text-gray-800">
    <header class="bg-white shadow p-4">
      <h1 class="text-2xl font-bold text-center">AI-Powered Company Explorer</h1>
    </header>

    <main class="container mx-auto mt-6 px-4">
      <%= yield %>
    </main>

    <footer class="text-center text-gray-500 text-sm mt-12 mb-4">
      &copy; <%= Time.current.year %> Pluto AI
    </footer>
  </body>
</html>
