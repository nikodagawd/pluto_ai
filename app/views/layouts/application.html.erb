<!DOCTYPE html>
<html>
  <head>
    <title>pluto.ai - AI-Powered Company Explorer</title>
    <%= favicon_link_tag %>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta property="og:title" content="pluto.ai - AI-Powered Company Explorer">
    <meta property="og:description" content="Discover innovative companies using AI-powered insights. Search by sector, location, funding stage, and more.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://www.plutonian.lol">
    <meta property="og:image" content="<%= image_url('favicon.ico') %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="m-0 p-0 w-full min-h-screen bg-transparent font-sans text-gray-800">

    <%= render "shared/navbar" %>


 <div class="min-h-screen w-full flex flex-col"
     style="
       background:
         radial-gradient(circle at 50% 50%, rgba(255,255,255,0.5) 0%, transparent 50%), /* luz central blanca */
         radial-gradient(circle at 20% 25%, rgba(59,130,246,0.19), transparent 45%),
         radial-gradient(circle at 75% 30%, rgba(124,58,237,0.19), transparent 50%),
         radial-gradient(circle at 40% 65%, rgba(59,130,246,0.1425), transparent 55%),
         radial-gradient(circle at 80% 70%, rgba(124,58,237,0.1425), transparent 60%),
         radial-gradient(circle at 50% 50%, rgba(255,255,255,0.38), transparent 30%),
         radial-gradient(circle at 50% 50%, rgba(255,255,255,0.2375), transparent 60%),
         radial-gradient(circle at 30% 80%, rgba(255,255,255,0.285), transparent 50%),
         radial-gradient(circle at 75% 60%, rgba(255,255,255,0.285), transparent 50%),
         #ffffff;
       background-blend-mode: normal;
       background-attachment: fixed;">


      <div class="<%= devise_controller? ? 'w-full flex-1 px-6' : 'w-full max-w-6xl mx-auto flex-1 px-4' %> bg-transparent">
        <%= yield %>
      </div>
    </div>


    <footer class="text-center text-gray-500 text-sm mt-12 mb-4">
      &copy; <%= Time.current.year %> Pluto AI
    </footer>
  </body>
</html>
